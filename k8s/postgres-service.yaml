# ClusterIP (the default type) — reachable only from inside the cluster.
# Deliberately no NodePort/LoadBalancer: Postgres must not be exposed externally.
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
