# Copy this file to .env and adjust values if you want credentials other than
# the local-dev defaults baked into docker-compose.yml. Never commit the real .env.

POSTGRES_DB=aeronex
POSTGRES_USER=aeronex
POSTGRES_PASSWORD=aeronex
